import React from 'react';
// import notImage from '../../assets/noteImage.jpg';

import Card from '../Card/Card';

const Home = ({ shoes }) => {

	return (
		<Card shoes={shoes} />
	);
};

export default Home;